#pragma once
#include <vector>

using cgeneratorlist_t = std::vector<std::pair<std::pair<int, int>, std::vector<float>>>;
using pencolor_t = std::vector<float>;
using relation_t = int;
using crosspoint_t = int;
